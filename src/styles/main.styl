@import "components/*";

.scrollable
    &__input
        position fixed
        top 80px
        right 20px
        width 100px
    &__counter
        position fixed
        top 84px
        right 140px
        text-shadow 0 0 4px white, 0 0 2px white
        font-size 18px

.scroll-content
    height 3000px
    background linear-gradient(rgba(black ,0) 0%, rgba(black, .65) 100%)
